server:
  port: 9090
spring:
  mvc:
    view:
      prefix: /views/
      suffix: .jsp
  datasource:
    url: jdbc:mysql://localhost:3306/insurance_report
    username: root
    password: Anish@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: --Some Gmail Account--
    password: --Some Password---
    default-encoding: UTF-8
    protocol: smtp
    properties:
      mail:
       smtp:
        auth: true
        starttls:
          enable: true